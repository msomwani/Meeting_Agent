# ── Version 1 only (local models) — not required for Version 2 ──────────────
whisperx                      # WhisperX ASR — faster Whisper + word-level alignment
pyannote.audio==3.1.1         # Speaker diarization
scipy                         # WAV file reading in diarize.py
numpy                         # array operations in diarize.py
torch                         # PyTorch — required by WhisperX + pyannote
torchaudio                    # Audio processing

# ── Version 2 only (cloud APIs) ──────────────────────────────────────────────
groq                          # Groq SDK — Whisper API transcription (whisper-large-v3)
assemblyai                    # AssemblyAI SDK — speaker diarization

langgraph                     # Multi-agent graph framework
langchain                     # Required by langfuse.langchain.CallbackHandler
langchain-core                # Base LangChain abstractions
langchain-groq                # Groq LLM integration
langfuse>=3.0                 # LLM observability — traces all agent node calls (v3+ API required)

fastapi                       # REST + WebSocket server
uvicorn[standard]             # ASGI server — [standard] includes websockets support
python-multipart              # Required for FastAPI file uploads
httpx                         # Async HTTP client — used by FastAPI TestClient

sqlalchemy                    # ORM for SQLite

pydantic>=2.0                 # Validates all LLM outputs — prevents silent failures

python-dotenv                 # Loads .env file
psutil                        # RAM detection in start.py